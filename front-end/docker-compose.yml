version: "3.8"

services:
  front-end:
    build:
      context: ..
      dockerfile: front-end/Dockerfile
    container_name: teddy-front
    restart: always
    ports:
      - "${FRONT_PORT:-5173}:80"
    environment:
      - API_URL=${API_URL:-host.docker.internal:3000}
